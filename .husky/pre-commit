#!/bin/sh

# Run linters
echo "🔍 Running ESLint..."
yarn lint || exit 1

echo "✨ Running Prettier check..."
yarn format:check || exit 1

# Run tests
echo "🧪 Running tests..."
yarn test:run || exit 1

echo "✅ All checks passed!"
